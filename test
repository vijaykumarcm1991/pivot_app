Before we move to the next phase, I'd like to review the Pivot Configuration contract that was implemented.

Please do not generate an example. Instead, show me the actual JSON structure that the frontend sends to the backend when I click "Generate Pivot".

Include:

1. The complete request body.
2. A description of every field.
3. Which fields are required and which are optional.
4. The validation rules for each field.
5. The response returned by the backend after validation.
6. The URL and HTTP method of the API endpoint.
7. If any default values are applied automatically, include those as well.

If the JSON is built dynamically in JavaScript, show the exact object before it is sent to the backend.

If there are any enums (such as aggregation types, layouts, date grouping, sorting, etc.), list every supported value.

Finally, explain how this JSON will be used by the pivot engine in the next phase.


Please show me:

- pivot_builder.html
- pivot-builder.js
- the backend route that receives the request
- the Pydantic model or schema used for validation
- the final JSON payload captured from a successful test

I want to verify that the frontend, backend and schema are all using the same contract.


save this as a markdown file